import * as Redux from 'redux';

export const Reducers:Redux.ReducersMapObject = {
    'persistance': function (state, action) { return state; },
    'undo': function (state, action) { return state; }
};
